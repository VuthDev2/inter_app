import { Suspense } from "react";
import SessionDetail from "@/components/SessionDetail";
export default function InsideTwoWayRecordPage() { return <Suspense fallback={null}><SessionDetail /></Suspense>; }
