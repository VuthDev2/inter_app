# Local setup

Install workspace dependencies from the repository root:

```sh
npm install
```

Run the website:

```sh
npm run web
```

Run the mobile app:

```sh
npm run mobile
```

Run iOS from the command line with `npm run ios`, or open `apps/mobile/ios/QuickVoice.xcworkspace` in Xcode and press Command-R.
